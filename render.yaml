services:
  - type: web
    name: flask-app
    env: python
    buildCommand: pip install -r requirements.txt && flask db upgrade
   startCommand: "gunicorn run:app"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: JWT_SECRET_KEY
        value: your_jwt_secret_key
      - key: MAIL_USERNAME
        value: vintageauction4@gmail.com
      - key: MAIL_PASSWORD
        value: cdud eapr jxqp iinw
